# 이 파일을 terraform.tfvars로 복사해서 채운다. terraform.tfvars는 커밋하지 않는다(.gitignore 처리됨).

allowed_ssh_cidrs = [
  "203.0.113.10/32", # tobby IP
  "203.0.113.11/32", # janna IP
  "203.0.113.12/32", # 교육장 IP
]
ssh_key_path = "~/.ssh/bakkucca_v1.pub"
